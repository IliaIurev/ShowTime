# Образ контейнера
FROM node:alpine

# Рабочая дирректория
WORKDIR /app

# Копируем файлы package.json в текущую дирректорию
COPY package*.json ./

# Установка зависимостей
RUN npm install

# Копируем ВСЕ файлы из текущей директории хоста
COPY . .

# Порт
EXPOSE 3000

# Команда для запуска приложения при старте контейнера
CMD ["npm", "start"]