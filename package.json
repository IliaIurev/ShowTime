{
  "name": "time-server",
  "version": "1.0.0",
  "description": "Simple time server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
